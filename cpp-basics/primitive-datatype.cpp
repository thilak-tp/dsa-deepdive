#include<iostream>
//The basic data types and their sizes in C++
using namespace std;

int main()
{
  int interger;
  char character;
  float floatingPoint;
  double dFloatingPoint;
  bool boolean;
  
  cout<<"The size of Interger is "<<sizeof(interger)<<endl<<"The size of char is "<<sizeof(character)<<endl<<"The size of float is "<<sizeof(floatingPoint)<<endl<<"The size of double is "<<sizeof(dFloatingPoint)<<endl<<"The size of a boolean is "<<sizeof(boolean);

  return 0;
}
